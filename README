# Proj2

to compile and run program type the following into the terminal (will require make)
replace <file> with input file name

    make file=<file>

    example: make file=input.txt

if above does not work then do the following
replace <input-file> with input file name

    compile:
        g++ proj2.cpp graph.cpp fileParse.cpp

    run:
        ./a.out <input-file>

        example: ./a.out input.txt

